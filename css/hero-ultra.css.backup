/* ===================================
   ULTRA-MODERN PREMIUM HERO SECTION
   =================================== */

.hero-section {
    min-height: 90vh;
    height: auto;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    padding: 140px 0 60px;
    background: linear-gradient(135deg, #FAFFFE 0%, #F0FDF4 50%, #E8F5E9 100%);
}

/* Modern Accent Shapes */
.hero-gradient-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
}

.hero-gradient-bg::before {
    content: '';
    position: absolute;
    top: -20%;
    right: -10%;
    width: 800px;
    height: 800px;
    background: radial-gradient(circle, rgba(0, 221, 95, 0.15) 0%, transparent 70%);
    border-radius: 50%;
}

.hero-gradient-bg::after {
    content: '';
    position: absolute;
    bottom: -30%;
    left: -15%;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(0, 177, 79, 0.1) 0%, transparent 70%);
    border-radius: 50%;
}

/* Decorative Elements */
.hero-grid-overlay {
    position: absolute;
    inset: 0;
    z-index: 0;
    opacity: 0.4;
}

.hero-grid-overlay::before {
    content: '';
    position: absolute;
    top: 10%;
    left: 5%;
    width: 120px;
    height: 120px;
    border: 3px solid rgba(0, 177, 79, 0.2);
    border-radius: 24px;
    transform: rotate(15deg);
}

.hero-grid-overlay::after {
    content: '';
    position: absolute;
    bottom: 15%;
    right: 8%;
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, rgba(0, 221, 95, 0.1), rgba(0, 177, 79, 0.15));
    border-radius: 50%;
}

.hero-content {
    position: relative;
    z-index: 1;
}

/* Main Hero Layout */
.hero-main {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
    margin-bottom: 2rem;
    width: 100%;
}

/* Text Content */
.hero-text-content {
    max-width: 100%;
    text-align: left !important;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    justify-content: flex-start;
    align-items: flex-start;
    padding-right: 2rem;
}

.hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: rgba(0, 177, 79, 0.1);
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--primary-color);
    margin-bottom: 1.5rem;
    animation: fadeInUp 0.8s ease;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    align-self: flex-start;
}

.eyebrow-dot {
    width: 6px;
    height: 6px;
    background: var(--primary-color);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
    0%, 100% {
        opacity: 1;
        transform: scale(1);
    }
    50% {
        opacity: 0.6;
        transform: scale(1.2);
    }
}

.hero-headline {
    font-size: 4rem;
    font-weight: 900;
    line-height: 1.2;
    color: #1a1a1a;
    margin-bottom: 1.5rem;
    letter-spacing: -0.03em;
    animation: fadeInUp 0.8s ease 0.2s backwards;
    text-align: left;
}

.gradient-text {
    background: linear-gradient(120deg, #00B14F 0%, #00D35F 50%, #00FF7F 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
    display: inline-block;
    font-weight: 900;
}

.hero-description {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    animation: fadeInUp 0.8s ease 0.4s backwards;
    text-align: left;
}

/* Inline Stats */
.hero-stats-inline {
    display: flex;
    gap: 3rem;
    margin-bottom: 3rem;
    padding: 2rem 0;
    border-top: 2px solid rgba(0, 177, 79, 0.1);
    border-bottom: 2px solid rgba(0, 177, 79, 0.1);
    animation: fadeInUp 0.8s ease 0.6s backwards;
    justify-content: flex-start;
    align-items: flex-start;
}

.stat-inline-item {
    display: flex;
    flex-direction: column;
}

.stat-value {
    font-size: 2.5rem;
    font-weight: 800;
    background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    margin-bottom: 0.5rem;
}

.stat-label {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Premium CTA Buttons */
.hero-cta-group {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    animation: fadeInUp 0.8s ease 0.8s backwards;
    justify-content: flex-start;
    align-items: flex-start;
}

.btn-premium {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 700;
    border-radius: 14px;
    text-decoration: none;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
}

.btn-premium::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.3), transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
}

.btn-premium:hover::before {
    opacity: 1;
}

.btn-premium.primary {
    background: linear-gradient(135deg, #00B14F 0%, #00D35F 100%);
    color: white;
}

.btn-premium.primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 30px rgba(0, 177, 79, 0.4);
}

.btn-premium.secondary {
    background: rgba(0, 177, 79, 0.05);
    color: var(--primary-color);
    border: 2px solid rgba(0, 177, 79, 0.2);
}

.btn-premium.secondary:hover {
    background: white;
    border-color: var(--primary-color);
    transform: translateY(-3px);
    box-shadow: 0 8px 30px rgba(0, 177, 79, 0.2);
}

.btn-premium i {
    transition: transform 0.3s ease;
}

.btn-premium:hover i {
    transform: translateX(4px);
}

/* Bike Showcase */
.hero-visual-content {
    position: relative;
    animation: fadeInRight 1s ease 0.4s backwards;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-end;
    justify-self: end;
    width: 100%;
}

@keyframes fadeInRight {
    from {
        opacity: 0;
        transform: translateX(50px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

.bike-showcase {
    position: relative;
    height: 600px;
}

.bike-card-premium {
    position: relative;
    width: 100%;
    height: 100%;
    background: white;
    border-radius: 32px;
    padding: 3rem;
    box-shadow:
        0 20px 60px rgba(0, 0, 0, 0.08),
        0 0 0 1px rgba(0, 177, 79, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.bike-image-main {
    width: 100%;
    height: auto;
    object-fit: contain;
    filter: drop-shadow(0 20px 40px rgba(0, 0, 0, 0.15));
    animation: bikeFloat 6s ease-in-out infinite;
    position: relative;
    z-index: 2;
}

@keyframes bikeFloat {
    0%, 100% {
        transform: translateY(0px) rotate(0deg);
    }
    25% {
        transform: translateY(-15px) rotate(-1deg);
    }
    50% {
        transform: translateY(-20px) rotate(0deg);
    }
    75% {
        transform: translateY(-15px) rotate(1deg);
    }
}

.bike-glow {
    position: absolute;
    bottom: 20%;
    left: 50%;
    transform: translateX(-50%);
    width: 70%;
    height: 40%;
    background: radial-gradient(ellipse, rgba(0, 177, 79, 0.3) 0%, transparent 70%);
    filter: blur(40px);
    animation: glowPulse 3s ease-in-out infinite;
    z-index: 1;
}

@keyframes glowPulse {
    0%, 100% {
        opacity: 0.6;
        transform: translateX(-50%) scale(1);
    }
    50% {
        opacity: 1;
        transform: translateX(-50%) scale(1.1);
    }
}

/* Floating Spec Cards */
.floating-spec-card {
    position: absolute;
    background: white;
    padding: 1.25rem 1.5rem;
    border-radius: 16px;
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.12);
    display: flex;
    align-items: center;
    gap: 1rem;
    border: 2px solid rgba(0, 177, 79, 0.1);
    animation: floatCard 4s ease-in-out infinite;
    z-index: 3;
}

@keyframes floatCard {
    0%, 100% {
        transform: translateY(0px);
    }
    50% {
        transform: translateY(-12px);
    }
}

.floating-spec-card.top-right {
    top: 10%;
    right: -5%;
    animation-delay: 0s;
}

.floating-spec-card.bottom-left {
    bottom: 10%;
    left: -5%;
    animation-delay: 2s;
}

.spec-icon {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.25rem;
}

.spec-info {
    display: flex;
    flex-direction: column;
}

.spec-value {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--text-primary);
    line-height: 1;
    margin-bottom: 0.25rem;
}

.spec-name {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

/* Features Marquee */
.features-marquee {
    width: 100%;
    overflow: hidden;
    background: white;
    padding: 2rem 0;
    border-top: 2px solid rgba(0, 177, 79, 0.1);
    border-bottom: 2px solid rgba(0, 177, 79, 0.1);
    position: relative;
    margin-top: 5rem;
    margin-left: calc(-50vw + 50%);
    margin-right: calc(-50vw + 50%);
    width: 100vw;
}

.marquee-track {
    display: flex;
    gap: 4rem;
    animation: marqueeScroll 30s linear infinite;
    width: max-content;
}

@keyframes marqueeScroll {
    0% {
        transform: translateX(0);
    }
    100% {
        transform: translateX(-50%);
    }
}

.marquee-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-primary);
    white-space: nowrap;
}

.marquee-item i {
    font-size: 1.25rem;
    color: var(--primary-color);
}

/* Duplicate marquee items for infinite scroll */
.marquee-track::after {
    content: attr(data-clone);
}

/* ===================================
   RESPONSIVE DESIGN
   =================================== */

@media (max-width: 1024px) {
    .hero-main {
        grid-template-columns: 1fr;
        gap: 4rem;
    }

    .hero-headline {
        font-size: 3.5rem;
    }

    .bike-showcase {
        height: 500px;
    }

    .hero-stats-inline {
        gap: 2rem;
    }
}

@media (max-width: 768px) {
    .hero-section {
        padding: 120px 0 80px;
    }

    .hero-headline {
        font-size: 2.75rem;
    }

    .hero-description {
        font-size: 1.125rem;
    }

    .hero-stats-inline {
        flex-direction: column;
        gap: 1.5rem;
    }

    .stat-inline-item {
        flex-direction: row;
        align-items: center;
        gap: 1rem;
    }

    .stat-value {
        font-size: 2rem;
    }

    .hero-cta-group {
        flex-direction: column;
    }

    .btn-premium {
        width: 100%;
        justify-content: center;
    }

    .bike-showcase {
        height: 400px;
    }

    .floating-spec-card {
        padding: 1rem;
    }

    .floating-spec-card.top-right {
        right: 0;
        top: 5%;
    }

    .floating-spec-card.bottom-left {
        left: 0;
        bottom: 5%;
    }

    .spec-value {
        font-size: 1.25rem;
    }

    .spec-name {
        font-size: 0.625rem;
    }
}

@media (max-width: 480px) {
    .hero-headline {
        font-size: 2.25rem;
    }

    .hero-eyebrow {
        font-size: 0.75rem;
        padding: 0.5rem 1rem;
    }

    .hero-stats-inline {
        padding: 1.5rem 0;
    }

    .stat-value {
        font-size: 1.75rem;
    }

    .marquee-item {
        font-size: 0.875rem;
    }

    .bike-showcase {
        height: 350px;
    }
}
